# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=3306                          # Default MySQL port. If different, check MySQL Workbench connection settings
DB_USER=root                          # Your MySQL username
DB_PASSWORD=your_mysql_password_here  # Replace with your actual MySQL password
DB_NAME=documents_management

# How to configure:
# 1. Copy this file and rename it to .env (remove .example)
# 2. Update DB_PASSWORD with your MySQL password
# 3. If your MySQL uses a different port (check Workbench), update DB_PORT
# 4. Save and restart the server

# Note: Never commit the .env file to GitHub!
# This is just an example template.
